<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<btn><%= link_to "Editer", edit_task_path %></btn>
<btn><%= link_to "Delete #{@task.title}", task_path(@task), method: :delete, data: { confirm: "Are you sure?" } %></btn>
